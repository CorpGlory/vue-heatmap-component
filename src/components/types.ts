export type HeatmapData = {
  x: string,
  y: string,
  value: number
}

export type ColorRange = {
  min: string,
  max: string
}

export type ValueRange = {
  min: number,
  max: number
}

export type Margin = {
  top: number,
  right: number,
  bottom: number,
  left: number
}
